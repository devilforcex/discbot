# Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# API URL for production builds
# In development, Vite proxies /api to the dashboard (see vite.config.ts)
# For production, set this to your dashboard domain (e.g., https://api.example.com)
VITE_API_URL=